

const requests = {
    addTask: (name, start, due, priority, description) => {

    },
    editTask: (name, start, due, priority, desscription) => {

    },
    completeTask: (id) => {
        
    },
};

export default requests;
